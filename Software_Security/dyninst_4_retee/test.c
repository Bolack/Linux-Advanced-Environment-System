#include <stdio.h>
#include <time.h>

int main()
{
    // printf("Hello world! Pid is %d\n", getpid());
    // sleep(1);
    for (int i = 0; i < 10; i++)
    {
        printf("%d log  \n", i);
    }
    printf("hello \n");
    return 0;
}